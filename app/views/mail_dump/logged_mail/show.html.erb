<% params[:as] ||= "html" %>
<%= render 'headers' %>

<% if params[:as] == "html" && @mail.has_html? %>
  <%== @mail.html.body.to_s %>
<% else %>
  <pre id="message_body">
    <% if @mail.has_text? %>
      <%= @mail.body.body.to_s %>
    <% else %>
      (no body)
    <% end %>
  </pre>
<% end %>
